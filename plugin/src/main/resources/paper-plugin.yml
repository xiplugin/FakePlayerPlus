name: FakePlayerPlus
version: ${version}
main: com.coderxi.plugin.fakeplayer.FakePlayerPlusPlugin
api-version: '1.21.11'
authors:
  - coderxi
website: 'https://github.com/xiplugin/FakePlayerPlus'

folia-supported: true

dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
    OpenInv:
      load: BEFORE
      required: false